# ── Core dependencies ─────────────────────────────────────────────────────────
# Generic versions — no platform-specific build tags (no +cpu, +mps, +cu121, etc.)
# The correct CUDA-enabled build is installed separately; see instructions below.

opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.2.0
tqdm>=4.65.0

# ── PyTorch (generic — works on all platforms) ────────────────────────────────
# Install the generic build for CPU / Apple Silicon (MPS):
#   pip install torch>=2.2 torchvision>=0.17 torchaudio>=2.2
#
# ── NVIDIA CUDA (recommended for training on Linux / Windows) ─────────────────
# Install the CUDA 12.1 build:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# For CUDA 11.8:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# Verify your installation:
#   python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
torch>=2.2
torchvision>=0.17
